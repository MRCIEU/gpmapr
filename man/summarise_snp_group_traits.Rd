% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snp_group_verification.R
\name{summarise_snp_group_traits}
\alias{summarise_snp_group_traits}
\title{Top Associated Traits Per SNP Group}
\usage{
summarise_snp_group_traits(
  trait_matrix,
  groups,
  trait_info = NULL,
  coloc_groups = NULL,
  exclude_trait_ids = NULL,
  min_group_size = 5L,
  top_n = 10L,
  min_snps_with_signal = 3L,
  n_categories = 2L
)
}
\arguments{
\item{trait_matrix}{SNPs x traits numeric matrix (e.g. oriented trait matrix from bivariate / EBMF verification helpers).}

\item{groups}{Named vector (\code{names} = SNP ids) or dataframe with \code{snp_id}
plus \code{group} / \code{cluster} / \code{program}.}

\item{trait_info}{Optional dataframe with \code{feature_id}/\code{trait_id} and
\code{feature_name}/\code{trait_name} columns for labels.}

\item{coloc_groups}{Optional coloc-group dataframe used to attach
\code{trait_category} labels (and for category summaries).}

\item{exclude_trait_ids}{Optional trait ids to drop (e.g. the target trait).}

\item{min_group_size}{Only summarise groups with more than this many SNPs.}

\item{top_n}{Maximum traits per group. Defaults to 10.}

\item{min_snps_with_signal}{Drop traits observed in fewer than this many SNPs
within the group. Defaults to 3.}

\item{n_categories}{Number of leading trait categories per group. Defaults to 2.}
}
\value{
A list with \code{by_group} (per-group trait tables and categories) and
\code{summary} (top trait and top categories per group).
}
\description{
For each SNP group larger than \code{min_group_size}, rank trait
columns by mean absolute effect among member SNPs and report the leading
trait categories. Prefer \code{summarise_module_specific_traits()} when
common trait categories dominate magnitude rankings.
}
