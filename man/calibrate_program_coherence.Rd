% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ebmf_program_validation.R
\name{calibrate_program_coherence}
\alias{calibrate_program_coherence}
\title{Permutation-Calibrate EBMF Program Coherence}
\usage{
calibrate_program_coherence(
  s_matrix,
  f_pm,
  n_perm = 2000L,
  edge_threshold = 0.2,
  seed = 1,
  chunk_size = 500L
)
}
\arguments{
\item{s_matrix}{SNP-by-SNP similarity matrix.}

\item{f_pm}{SNP-by-program posterior mean loading matrix (\code{flash_fit$F_pm}).}

\item{n_perm}{Number of permutations per program. Defaults to \code{2000}.}

\item{edge_threshold}{Absolute similarity defining an edge for the weighted
quorum statistic. Defaults to \code{0.2}.}

\item{seed}{RNG seed.}

\item{chunk_size}{Permutations evaluated per matrix product, bounding peak
memory. Defaults to \code{500}.}
}
\value{
A dataframe with one row per program: \code{program},
\code{weighted_internal}, \code{weighted_quorum}, \code{n_eff}, \code{coherence_emp_p},
\code{coherence_q}, \code{quorum_emp_p} and \code{quorum_q}.
}
\description{
Test whether each program's loading vector is aligned with the
SNP similarity graph more than a random reassignment of the same loadings
would be. For every program the weights \code{w = F_pm[, k]^2} are permuted across
the SNP index \code{n_perm} times and the loading-weighted coherence recomputed,
giving an empirical p-value and a BH-adjusted q-value.

This replaces comparing \code{mean_internal_similarity} to an absolute constant.
Because permuting \code{w} preserves its distribution, the null is automatically
matched on program size and on the shape of the loading vector, so the same
threshold transfers between datasets whose similarity graphs have different
baselines — which absolute thresholds do not.
}
