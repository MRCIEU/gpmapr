% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathways.R
\name{enrich_snp_group_pathways}
\alias{enrich_snp_group_pathways}
\title{Pathway Enrichment For SNP Groups}
\usage{
enrich_snp_group_pathways(
  groups,
  coloc_groups,
  min_group_size = 5L,
  snp_key = c("variant_id", "display_snp", "coloc_group_id"),
  sources = c("KEGG", "Reactome"),
  p_value_threshold = 0.05,
  minimum_count_in_network = 2L
)
}
\arguments{
\item{groups}{Either a named vector (\code{names} = SNP ids, values = group ids)
or a dataframe with \code{snp_id} plus a group column (\code{group}, \code{cluster}, or
\code{program}).}

\item{coloc_groups}{Coloc-group dataframe used to map SNPs to genes.}

\item{min_group_size}{Only enrich groups with at least this many SNPs.
Defaults to 5.}

\item{snp_key}{Column used to match SNP ids in \code{coloc_groups}.}

\item{sources}{Pathway sources; see \code{enrich_trait_pathways()}. Non-HP sources
(e.g. KEGG, Reactome) are summarised as pathways; HP (Human Phenotype
Ontology) hits are summarised separately as phenotypes.}

\item{p_value_threshold}{FDR threshold passed to \code{pathway_enrichment()}.}

\item{minimum_count_in_network}{Minimum overlap passed to \code{pathway_enrichment()}.}
}
\value{
A list with:
\itemize{
\item by_group: list of per-group results (\code{group}, \code{n_snps}, \code{genes},
\code{snp_genes}, \code{pathways})
\item summary: one row per enriched group. \code{n_enriched_pathways} /
\code{top_enriched_pathway} refer to non-HP sources only;
\code{n_enriched_phenotypes} / \code{top_enriched_phenotype} refer to HP terms
only (NA / 0 when \code{sources} has no HP).
}
}
\description{
Verification helper: for each SNP grouping (Louvain module,
EBMF program, etc.) with more than \code{min_group_size} SNPs, collect molecular
QTL genes at those SNPs and test pathway enrichment. Compare against
\code{enrich_trait_pathways()} to see whether baseline pathways reappear and
whether they split cleanly across groups.
}
