% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multitrait_concordance.R
\name{compare_program_pairs_profiles}
\alias{compare_program_pairs_profiles}
\title{Compare EBMF Programs Across Traits On Their Trait Profiles}
\usage{
compare_program_pairs_profiles(
  program_data,
  n_perm = 1000L,
  fdr_threshold = 0.05,
  seed = 1
)
}
\arguments{
\item{program_data}{An \code{extract_program_loadings()} result or a list of them.
Requires the \code{profiles} component, which \code{extract_program_loadings()}
populates from the EBMF fit's \code{L_pm}.}

\item{n_perm}{Permutations for the null. Defaults to \code{1000}.}

\item{fdr_threshold}{BH threshold defining the candidate tier. Defaults to
\code{0.05}.}

\item{seed}{RNG seed.}
}
\value{
A list with \code{pairs}, \code{matches} and \code{settings}, matching the shape of
\code{compare_program_pairs_loadings()} but with feature-axis columns
(\code{n_features_axis}, \code{profile_concordance}, \code{cosine_features},
\code{n_eff_features}).
}
\description{
Score every cross-trait pair of programs on how similarly they
load across the background studies the two traits have in common, and
calibrate that against a permutation null.

This is the sibling of \code{compare_program_pairs_loadings()}, which scores pairs
on shared \emph{loci}. That statistic is an inner product over the loci both
traits carry, so two programs describing the same biology at different loci
score near zero by construction — and when two traits share few loci (BMI and
height share 73 of 3456) every locus-axis result rests on very little.
Matching on the trait profile drops the requirement that the programs act at
the same variants: two programs correspond when the same background studies
load on them, whatever loci carry that signal in each trait.

Read \code{n_eff_features} alongside \code{concordance_z} exactly as \code{n_eff_loci} is
read for the locus axis: a large z resting on two or three effective
features is thin.
}
