% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snp_clustering.R
\name{summarise_snp_module_quality}
\alias{summarise_snp_module_quality}
\title{SNP Module Reliability Metrics}
\usage{
summarise_snp_module_quality(
  s_matrix,
  cluster,
  edge_threshold = 0.5,
  min_module_size = 3L,
  min_mean_internal = 0.3,
  min_connectedness = 0.5
)
}
\arguments{
\item{s_matrix}{Symmetric SNP-by-SNP cosine similarity matrix.}

\item{cluster}{Named integer vector of cluster assignments (\code{names} = SNP ids).}

\item{edge_threshold}{Absolute similarity used for connectedness and
connected-component counts. Defaults to \code{0.5}.}

\item{min_module_size}{Minimum SNPs for \code{reliable} to be \code{TRUE}. Defaults to 3.}

\item{min_mean_internal}{Minimum mean internal similarity for \code{reliable}.
Defaults to \code{0.3}.}

\item{min_connectedness}{Minimum pair-connectedness for \code{reliable}.
Defaults to \code{0.5}.}
}
\value{
A dataframe, one row per module, with internal/external similarity,
connectedness, component coverage, diagnostic silhouette, and a \code{reliable}
flag.
}
\description{
Graph-quality diagnostics for SNP modules on a cosine similarity
matrix. For each module this reports internal vs external similarity,
connectedness after applying \code{edge_threshold} to \verb{|S|}, and a diagnostic
silhouette. Use size, internal similarity, and connectedness to drop weakly
linked modules rather than keeping every Louvain community.

Connectedness is the fraction of unique SNP pairs in the module with
\eqn{|S| \ge} \code{edge_threshold}. Silhouette uses distance \eqn{1 - S} and is
reported only as a diagnostic: because SNPs can participate in multiple
biological programs, negative silhouettes are expected and are not used to
gate \code{reliable}.
}
