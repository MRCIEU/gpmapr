% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_trait.R
\name{real_bmi_graph_targets}
\alias{real_bmi_graph_targets}
\title{Real Similarity-Graph Calibration Targets}
\usage{
real_bmi_graph_targets()
}
\value{
A one-row dataframe with the same columns as
\code{simulated_graph_diagnostics()} returns, where measured.
}
\description{
Graph statistics measured on the BMI pleiotropy matrix (1002
traits x 970 SNPs, \code{trait_subset = "phenotypic"}, \code{min_snp_signals = 2}).
These are the values \code{simulated_graph_diagnostics()} should land near for a
simulation to exercise the validation gates in the regime real data puts
them in.

The two that matter most are \code{median_pair_overlap} (a real SNP pair shares
only about three traits, so each cosine rests on very little) and
\code{mean_similarity_no_target} (real background traits are correlated with each
other, so similarity survives removing the target row).
}
