% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multitrait_concordance.R
\name{shared_feature_universe}
\alias{shared_feature_universe}
\title{Shared Feature Universe Between Traits}
\usage{
shared_feature_universe(program_data)
}
\arguments{
\item{program_data}{An \code{extract_program_loadings()} result or a list of them.}
}
\value{
A dataframe with one row per trait pair: \code{trait_id_a}, \code{trait_id_b},
\code{n_features_a}, \code{n_features_b}, \code{n_features_shared}, \code{frac_shared}, and
\code{n_loci_shared} for the locus axis alongside it.
}
\description{
Count the background studies that two traits' EBMF fits have in
common. This is the axis \code{compare_program_pairs_profiles()} scores on, and
it is deliberately reported before any comparison: the whole reason to match
programs on their trait profile rather than on shared loci is that the
feature axis should be far wider than the locus intersection. Where it is
not, profile matching has no more support than locus matching and the result
should be read accordingly.
}
