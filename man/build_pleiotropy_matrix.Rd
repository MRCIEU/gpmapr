% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pleiotropy_matrix.R
\name{build_pleiotropy_matrix}
\alias{build_pleiotropy_matrix}
\title{Build Pleiotropy Matrix}
\usage{
build_pleiotropy_matrix(
  trait_id,
  coloc_groups = NULL,
  p_threshold = NULL,
  snp_key = c("variant_id", "display_snp", "coloc_group_id")
)
}
\arguments{
\item{trait_id}{Numeric ID of the target trait whose associated SNPs define the columns.}

\item{coloc_groups}{A dataframe of coloc_groups with \code{beta} and \code{se} columns
(from \code{trait(..., include_associations = TRUE)} or \code{traits(...)}). If NULL,
will fetch via \code{trait(trait_id, include_associations = TRUE)}.}

\item{p_threshold}{P-value threshold for including a target-trait SNP. Defaults to 5e-8.}

\item{snp_key}{Column used to name SNP columns: \code{"variant_id"}, \code{"display_snp"}, or
\code{"coloc_group_id"}. Defaults to \code{"variant_id"}.}
}
\value{
A list with:
\itemize{
\item x_matrix: numeric matrix (traits x SNPs) of z-scores; \code{NA} where a trait
has no colocalisation signal at that SNP's locus
\item trait_info: dataframe mapping row indices to \code{trait_id} and \code{trait_name}
\item snp_info: dataframe mapping column names to \code{coloc_group_id}, \code{variant_id},
\code{display_snp}, \code{chr}, and \code{bp}
\item target_trait_id: the target trait ID used
}
}
\description{
Construct a traits x SNPs matrix of signed z-scores (beta / se) from
colocalisation data. Each column is a target-trait SNP; each row is a background
trait with a colocalisation signal at that SNP's locus.
}
