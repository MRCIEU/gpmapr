% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pleiotropy_matrix.R
\name{cluster_snp_profiles}
\alias{cluster_snp_profiles}
\title{Cluster SNPs by Pleiotropy Profile Similarity}
\usage{
cluster_snp_profiles(
  s_matrix,
  k = 3,
  method = c("hierarchical", "spectral", "community", "gmm"),
  x_matrix = NULL,
  linkage = "average",
  community_algorithm = c("leading_eigen", "louvain"),
  na_as_zero = TRUE,
  n_latent = NULL
)
}
\arguments{
\item{s_matrix}{Symmetric SNP-by-SNP cosine similarity matrix from
\code{snp_similarity_matrix()$s_matrix}.}

\item{k}{Number of clusters (biological modules). For Louvain community detection,
\code{k} is ignored and the number of modules is data-driven.}

\item{method}{Clustering method: \code{"hierarchical"}, \code{"spectral"}, \code{"community"},
or \code{"gmm"}.}

\item{x_matrix}{Oriented pleiotropy matrix (traits x SNPs). Required for
\code{method = "gmm"}; typically \code{orient_pleiotropy_matrix()$x_matrix}.}

\item{linkage}{Linkage method passed to \code{stats::hclust()} when
\code{method = "hierarchical"}. Defaults to \code{"average"}.}

\item{community_algorithm}{Community detection algorithm when
\code{method = "community"}: \code{"leading_eigen"} (recursive splits via \code{steps = k - 1})
or \code{"louvain"} (data-driven module count; requires the \strong{igraph} package).}

\item{na_as_zero}{Treat \code{NA} entries as zero when building latent coordinates
for GMM clustering.}

\item{n_latent}{Number of latent dimensions for GMM clustering. Defaults to
\code{min(k + 1, n_traits - 1, n_snps - 1)}.}
}
\value{
A list with:
\itemize{
\item cluster: named integer vector of cluster assignments per SNP
\item method: clustering method used
\item k: requested number of clusters (\code{NA} for Louvain)
\item n_clusters: number of clusters returned
\item details: method-specific objects (e.g. \code{hclust}, \code{kmeans}, \code{mclust})
}
}
\description{
Group SNPs into functional classes based on geometric parallelism of
their multi-trait profiles. Supports hierarchical clustering on \eqn{1 - S},
spectral clustering on the similarity graph, community detection, and Gaussian
mixture models in a latent space derived from the oriented pleiotropy matrix.
}
