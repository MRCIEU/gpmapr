% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ebmf_calibration.R
\name{stability_ebmf_programs}
\alias{stability_ebmf_programs}
\title{Stability of EBMF Programs Across Trait Subsamples}
\usage{
stability_ebmf_programs(
  clustering_result,
  n_rep = 10,
  frac_traits = 0.8,
  top_n = 20,
  seed = 1,
  verbose = TRUE
)
}
\arguments{
\item{clustering_result}{Result of \code{run_univariate_clustering()} with
\code{cluster_type = "ebmf"}.}

\item{n_rep}{Number of subsample replicates.}

\item{frac_traits}{Fraction of trait rows sampled per replicate.}

\item{top_n}{Size of each program's member set used for matching.}

\item{seed}{RNG seed.}

\item{verbose}{Print progress messages.}
}
\value{
A dataframe with columns \code{program}, \code{n_ref} (reference member-set
size), \code{replication} (mean best-overlap fraction), \code{sd_replication}.
}
\description{
Refit the EBMF pipeline on trait subsamples of the observed
matrix and score how consistently each program's member set reappears. For
each replicate, a random fraction of trait rows is held out, the fit is
repeated with the stored pipeline parameters, and each factor contributes
its top-loaded SNP set. A reference program's replication score is the mean
across replicates of its best overlap with any replicate factor set.
Descriptive: scores are not p-values. Use them to prioritise programs whose
membership survives resampling.
}
