% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/continuous_enrichment.R
\name{enrich_program_loadings_trait_categories}
\alias{enrich_program_loadings_trait_categories}
\title{Continuous Trait-Category Enrichment From EBMF Loadings}
\usage{
enrich_program_loadings_trait_categories(
  clustering_result,
  coloc_groups,
  snp_key = c("variant_id", "display_snp", "coloc_group_id"),
  min_category_size = 5L,
  min_loading_magnitude = 0
)
}
\arguments{
\item{clustering_result}{Result of \code{run_univariate_clustering()}.}

\item{coloc_groups}{Coloc-group dataframe with a \code{trait_category} column.}

\item{snp_key}{Column used to match SNP ids in \code{coloc_groups}.}

\item{min_category_size}{Only test a category if at least this many SNPs
link to it. Defaults to 5.}

\item{min_loading_magnitude}{Soft membership gate: a SNP only counts
towards a category's \code{x = 1} group if its absolute loading on that
program exceeds this value; otherwise it falls back to \code{x = 0} for that
program's test (it stays in the analysis, just not counted as a member).
This keeps near-zero, shrinkage-only loadings from diluting the mean
squared-loading contrast without imposing a hard lFSR/magnitude gate.
Defaults to 0.02.}
}
\value{
A list with:
\itemize{
\item by_program: list of per-program results (\code{program}, \code{n_snps},
\code{comparison} with columns trait_category, enrichment, se, z, p, fdr,
n_snps, n_category_snps; \code{fdr} is corrected across every program
tested, not within this program alone)
\item summary: one row per program (\code{n_categories_tested},
\code{n_enriched}, \code{top_category})
}
}
\description{
Parallel to \code{enrich_snp_group_trait_categories()}, but without
any hard lFSR/magnitude cutoff: every SNP with a finite EBMF loading
contributes to every program's test, weighted by its squared loading,
regressed against each trait category's local 0/1 membership indicator
(\code{lm(loading^2 ~ category_membership)}), following the approach in
\code{scripts/enrichment_via_loadings_example.r}. Every trait category tested for
every program is one Benjamini-Hochberg family, so \code{fdr} reflects the true
number of regressions run for this trait -- not one program's slice of
them. Tissue and pathway enrichment
(\code{enrich_program_loadings_tissues()}, \code{enrich_program_loadings_pathways()})
are each their own separate family; neither is pooled with this one.

IMPORTANT: as in that script, SNPs are treated as independent
observations; for formal inference, LD should be accounted for (e.g. SNP
-> gene aggregation, or cluster-robust SEs / permutation using LD blocks).
The enrichment effect estimates are nevertheless useful for comparison
against the hard-cutoff hypergeometric trait-category test.
}
