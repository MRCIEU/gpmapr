% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ebmf_clustering.R
\name{identify_ebmf_global_factors}
\alias{identify_ebmf_global_factors}
\title{Identify Global EBMF Factors To Drop}
\usage{
identify_ebmf_global_factors(
  flash_fit,
  membership = NULL,
  min_total_pve = 0.01,
  min_pve_share = 0.4,
  min_soft_frac = 0.85,
  max_drop = 1L
)
}
\arguments{
\item{flash_fit}{A \code{flash} object from \code{run_ebmf()}.}

\item{membership}{Optional SNP x program logical membership matrix from
\code{extract_ebmf_clusters()$membership}.}

\item{min_total_pve}{Require this total PVE before dropping anything.
Defaults to \code{0.01}.}

\item{min_pve_share}{Candidate if the factor explains at least this share of
total PVE and soft-assigns at least 50\\% of SNPs. Defaults to \code{0.4}.}

\item{min_soft_frac}{Candidate if the factor soft-assigns at least this
fraction of SNPs. Defaults to \code{0.85}.}

\item{max_drop}{Maximum number of factors to return. Defaults to 1.}
}
\value{
Integer vector of 1-based factor indices to pass to
\code{remove_ebmf_factors()}, possibly empty.
}
\description{
Suggest factor indices that look like a shared/global axis,
but only when the fit has non-trivial total PVE. Factor 1 is not assumed
global. Returns an empty integer vector for near-null fits. By default at
most one factor is suggested (highest PVE share among candidates).
}
