% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_multitrait_simulation.R
\name{map_programs_to_planted}
\alias{map_programs_to_planted}
\title{Map Discovered Programs Onto Planted Programs}
\usage{
map_programs_to_planted(
  loadings,
  ground_truth,
  by = c("snps", "drivers"),
  min_jaccard = 0.1,
  min_fold = 3
)
}
\arguments{
\item{loadings}{A \code{extract_program_loadings()} loadings table (SNP axis) or
profiles table (feature axis), depending on \code{by}.}

\item{ground_truth}{The \code{ground_truth} component of
\code{simulate_trait_pair()}.}

\item{by}{\code{"snps"} (default) or \code{"drivers"}.}

\item{min_jaccard}{Minimum Jaccard overlap for a \code{by = "snps"} mapping to be
accepted; below it the program is mapped to \code{NA}.}

\item{min_fold}{Minimum enrichment over chance for a \code{by = "drivers"}
mapping. The feature table is dense -- every background study carries some
loading on every program -- so a set overlap is the wrong measure there: the
eight planted drivers of a program are only eight of several hundred
features, and a Jaccard would reject a perfect match. Instead the score is
the share of the program's total squared feature loading falling on the
candidate driver set, divided by the share expected if loading were spread
evenly. Defaults to \code{3}.}
}
\value{
A dataframe with \code{program_id}, \code{trait_id}, \code{planted_label}, \code{score}
(Jaccard for \code{"snps"}, weighted loading share for \code{"drivers"}) and \code{fold}
(\code{NA} for \code{"snps"}).
}
\description{
For each discovered program, find the planted program of the same
trait that it best overlaps. Used to translate EBMF's arbitrary program
numbering into the planted labels before cross-trait links can be scored.

Mapping is done on SNP membership by default. Set \code{by = "drivers"} to map on
the program's loading over background studies instead, which is the right
choice when scoring the profile axis at low locus overlap: a program whose
loci were planted privately still has the planted driver studies loading on it.
}
