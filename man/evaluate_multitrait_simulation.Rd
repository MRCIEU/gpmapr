% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_multitrait_simulation.R
\name{evaluate_multitrait_simulation}
\alias{evaluate_multitrait_simulation}
\title{Score Cross-Trait Links Against The Planted Correspondence}
\usage{
evaluate_multitrait_simulation(pairs, mapping, ground_truth, tiers = "primary")
}
\arguments{
\item{pairs}{The \code{pairs} table from \code{compare_program_pairs_profiles()} or
\code{compare_program_pairs_loadings()}.}

\item{mapping}{Result of \code{map_programs_to_planted()}.}

\item{ground_truth}{The \code{ground_truth} component of \code{simulate_trait_pair()}.}

\item{tiers}{Link tiers to score. Defaults to \code{"primary"} only, which is the
calibrated one-to-one claim.}
}
\value{
A one-row dataframe: \code{n_links}, \code{n_true_positive},
\code{n_false_positive}, \code{precision}, \code{recall}, \code{f1}, \code{sign_accuracy},
\code{n_planted_shared}, \code{n_recovered_shared}.
}
\description{
Evaluate one cross-trait comparison against
\code{simulate_trait_pair()} ground truth. The estimand is the planted programme
correspondence: which program in trait A is the same program as which in trait
B, and whether the two traits are pushed the same way or opposite ways.

A discovered link is a true positive when both of its programs map to the
\strong{same} planted shared label. Recall is over planted shared programs, so
recovering one shared program through several discovered links counts once --
EBMF factor splitting should not inflate it. Precision is over discovered
links, so a method that links everything is penalised.

Sign accuracy is scored only on true positives: a link that is not real has no
meaningful direction.
}
