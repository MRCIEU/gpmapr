% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathways.R
\name{enrich_program_loadings_pathways}
\alias{enrich_program_loadings_pathways}
\title{Continuous Pathway Enrichment From EBMF Loadings}
\usage{
enrich_program_loadings_pathways(
  clustering_result,
  coloc_groups,
  mappings = NULL,
  snp_key = c("variant_id", "display_snp", "coloc_group_id"),
  sources = c("KEGG", "Reactome"),
  min_category_size = 5L,
  min_loading_magnitude = 0,
  include_situated_gene = FALSE
)
}
\arguments{
\item{clustering_result}{Result of \code{run_univariate_clustering()}.}

\item{coloc_groups}{Coloc-group dataframe used to map SNPs to genes.}

\item{mappings}{Optional pre-fetched \code{pathway_mappings()} result, reused
across programs/traits instead of re-querying the API. Defaults to
calling \code{pathway_mappings(source = NULL)}.}

\item{snp_key}{Column used to match SNP ids in \code{coloc_groups}.}

\item{sources}{Pathway sources to keep, e.g. \code{c("KEGG", "Reactome")}. \code{NULL}
keeps every source present in \code{mappings}. Sources are pooled into one FDR
family, not corrected independently.}

\item{min_category_size}{Only test a pathway if at least this many SNPs
map (via \code{genes_at_snps()}) to one of its genes. Defaults to 5.}

\item{min_loading_magnitude}{Soft membership gate: a SNP only counts
towards a pathway's \code{x = 1} group if its absolute loading on that
program exceeds this value; otherwise it falls back to \code{x = 0} for that
program's test (it stays in the analysis, just not counted as a
member). This keeps near-zero, shrinkage-only loadings from diluting
the mean squared-loading contrast without imposing a hard
lFSR/magnitude gate. Defaults to 0.02.}

\item{include_situated_gene}{Include situated-gene links in addition to
ordinary gene links when mapping SNPs to genes. Defaults to \code{FALSE}.}
}
\value{
A list with:
\itemize{
\item by_program: list of per-program results (\code{program}, \code{n_snps},
\code{comparison} with columns term_id, source, description, enrichment,
se, z, p, fdr, n_snps, n_category_snps; \code{fdr} is corrected across
every program and source tested -- \code{source} is a label only)
\item summary: one row per program (\code{n_pathways_tested}, \code{n_enriched},
\code{top_pathway})
\item mappings: the \code{pathway_mappings()} result used
}
}
\description{
Parallel to \code{enrich_snp_group_pathways()}, but without a hard
lFSR/magnitude cutoff: every SNP with a finite EBMF loading contributes to
every program's test, weighted by its squared loading, and is regressed
against each pathway's local 0/1 gene-membership indicator
(\code{lm(loading^2 ~ pathway_membership)}), following the approach in
\code{scripts/enrichment_via_loadings_example.r}. Pathway gene sets come from
\code{pathway_mappings()} (the full local gene x pathway universe) rather than
the overrepresentation-only \code{pathway_enrichment()} API. Every pathway term
tested for every program and every kept \code{source} (KEGG and Reactome pooled
together) is one Benjamini-Hochberg family, so \code{fdr} reflects the true
number of regressions run for this trait -- not one program's or one
source's slice of them. Trait-category, tissue, and pathway enrichment
(\code{enrich_program_loadings_trait_categories()},
\code{enrich_program_loadings_tissues()}, this function) are each their own
separate family; none of the three is pooled with another.

IMPORTANT: as in that script, SNPs are treated as independent
observations; for formal inference, LD should be accounted for (e.g. SNP
-> gene aggregation, or cluster-robust SEs / permutation using LD blocks).
The enrichment effect estimates are nevertheless useful for comparison
against the hard-cutoff hypergeometric pathway test.
}
