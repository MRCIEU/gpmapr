% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coloc_consistency.R
\name{net_directionality_score}
\alias{net_directionality_score}
\title{Net Directionality Score}
\usage{
net_directionality_score(
  trait_id_1,
  trait_id_2,
  coloc_groups = NULL,
  p_threshold = 5e-08
)
}
\arguments{
\item{trait_id_1}{Numeric trait ID for the first trait}

\item{trait_id_2}{Numeric trait ID for the second trait}

\item{coloc_groups}{A dataframe of coloc_groups with beta columns (from include_associations = TRUE).
If NULL, will fetch via traits(c(trait_id_1, trait_id_2), include_associations = TRUE).}

\item{p_threshold}{P-value threshold. Defaults to 5e-8.}
}
\value{
A list with:
\itemize{
\item nds: the Net Directionality Score (-1 to 1)
\item n_pos: number of concordant loci
\item n_neg: number of discordant loci
\item n_total: total shared loci with available betas
}
}
\description{
Computes the net directionality of effect between two traits at shared
colocalised loci. Uses association betas to determine concordance.

NDS = (n_pos - n_neg) / (n_pos + n_neg)
where:
\itemize{
\item n_pos = number of shared loci with concordant effect directions
\item n_neg = number of shared loci with discordant effect directions
}
}
