% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait_correlation.R
\name{trait_correlation_groups}
\alias{trait_correlation_groups}
\title{Trait correlation via coloc groups}
\usage{
trait_correlation_groups(traits, p_value_threshold = 5e-08, h4_threshold = 0.8)
}
\arguments{
\item{h4_threshold}{Minimum H4 used when fetching coloc pairs (default 0.8)}

\item{trait_a}{Trait id (numeric) or GUID for trait A}

\item{trait_b}{Trait id (numeric) or GUID for trait B}
}
\value{
A list:
\itemize{
\item pcs: Pleiotropic Consistency Score \eqn{n / (n + m_1 + m_2)}
\item nds: Net Directionality Score \eqn{(n_{pos} - n_{neg}) / (n_{pos} + n_{neg})}
\item n_shared: loci where both traits colocalise
\item m1: loci unique to trait A
\item m2: loci unique to trait B
\item n_pos: shared loci with concordant beta direction
\item n_neg: shared loci with discordant beta direction
\item locus_details: per-locus dataframe of betas and directions
}
}
\description{
Compare two traits using their shared colocalisation groups.
Returns a Pleiotropic Consistency Score (Jaccard-style) and a Net
Directionality Score based on concordance of effect directions at shared
loci. When \code{include_associations = TRUE} is passed through to the API,
the coloc_groups dataframe gains \code{beta} and \code{se} columns which
are used for the directionality score.
}
