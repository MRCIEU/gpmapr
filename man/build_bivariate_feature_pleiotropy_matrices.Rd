% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pleiotropy_matrix.R
\name{build_bivariate_feature_pleiotropy_matrices}
\alias{build_bivariate_feature_pleiotropy_matrices}
\title{Build Bivariate Feature Pleiotropy Matrices}
\usage{
build_bivariate_feature_pleiotropy_matrices(
  trait_id_1,
  trait_id_2,
  coloc_groups = NULL,
  p_threshold = NULL,
  snp_key = c("variant_id", "display_snp", "coloc_group_id"),
  pathway_source = NULL,
  pathway_p_value_threshold = 0.05,
  minimum_count_in_network = NULL
)
}
\arguments{
\item{trait_id_1}{Numeric ID of the first target trait (columns of \eqn{X_1}).}

\item{trait_id_2}{Numeric ID of the second target trait (columns of \eqn{X_2}).}

\item{coloc_groups}{A dataframe of coloc_groups with \code{beta} and \code{se} columns
covering both traits. If NULL, fetched via
\code{traits(c(trait_id_1, trait_id_2), include_associations = TRUE)}.}

\item{p_threshold}{P-value threshold for including target-trait SNPs. If NULL,
all SNPs with valid z-scores are retained.}

\item{snp_key}{Column used to name SNP columns. Defaults to \code{"variant_id"}.}

\item{pathway_source}{Optional pathway source passed to \code{pathway_enrichment()}.}

\item{pathway_p_value_threshold}{FDR threshold for pathway enrichment.}

\item{minimum_count_in_network}{Minimum gene overlap per pathway term.}
}
\value{
A list with aligned \code{x1_matrix}, \code{x2_matrix}, \code{feature_info}, \code{z_target_1},
\code{z_target_2}, SNP metadata, and the shared \code{feature_map}.
}
\description{
Like \code{build_bivariate_pleiotropy_matrices()}, but gene-linked molecular
QTL rows are collapsed into pathway/gene \strong{features} while standard background
trait rows are retained. Row names are aligned across both matrices.
}
