% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pleiotropy_matrix.R
\name{summarise_snp_modules}
\alias{summarise_snp_modules}
\title{Summarise SNP Module Phenotype Drivers}
\usage{
summarise_snp_modules(
  x_matrix,
  cluster,
  trait_info = NULL,
  exclude_trait_id = NULL,
  na_as_zero = TRUE,
  top_n = NULL
)
}
\arguments{
\item{x_matrix}{Oriented pleiotropy matrix (traits x SNPs), typically
\code{orient_pleiotropy_matrix()$x_matrix}.}

\item{cluster}{Named integer vector of cluster assignments per SNP column
(as returned by \code{cluster_snp_profiles()$cluster}).}

\item{trait_info}{Optional dataframe with \code{trait_id} and \code{trait_name} columns
(as returned by \code{build_pleiotropy_matrix()$trait_info}).}

\item{exclude_trait_id}{Optional trait ID to exclude from summaries (e.g. the
target trait when interpreting background drivers).}

\item{na_as_zero}{Treat \code{NA} entries as zero before averaging. Defaults to \code{TRUE}.}

\item{top_n}{If not \code{NULL}, return only the top \code{n} traits per cluster ranked by
\code{abs_mean_z}.}
}
\value{
A dataframe with columns: \code{cluster}, \code{trait_id}, \code{trait_name} (if
available), \code{mean_z}, \code{abs_mean_z}, \code{n_snps}.
}
\description{
For each SNP cluster, compute the mean oriented pleiotropy profile
across member SNPs and rank background traits by absolute effect magnitude to
identify functional drivers of each biological module.
}
