% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_mcse.R
\name{ademp_summarise}
\alias{ademp_summarise}
\title{Summarise Simulation Performance with Monte Carlo Error}
\usage{
ademp_summarise(
  data,
  value,
  group_cols = character(0),
  type = c("mean", "prop", "bias"),
  truth = NULL,
  level = 0.95
)
}
\arguments{
\item{data}{A data frame of per-repetition results.}

\item{value}{Name (string) of the column holding the per-repetition
performance measure.}

\item{group_cols}{Character vector of column names to group by (e.g.
\code{"version"}). Use \code{character(0)} for an overall summary.}

\item{type}{Type of performance measure: \code{"mean"} for a continuous measure,
\code{"prop"} for a proportion (binary measure), or \code{"bias"} for a bias
estimate (requires \code{truth}).}

\item{truth}{Numeric scalar: true value of the estimand; required when
\code{type = "bias"}.}

\item{level}{Confidence level for the Monte Carlo interval (default 0.95).}
}
\value{
A data frame with the grouping columns plus \code{n}, \code{estimate}, \code{mcse},
\code{conf_low}, \code{conf_high}, \code{type}, and \code{level}.
}
\description{
Group-wise summary of a performance measure across simulation
repetitions, reporting the estimate, its Monte Carlo standard error, and a
Monte Carlo confidence interval (Morris, White & Crowther 2019). This is
the ADEMP "performance measures" dataset builder.
}
