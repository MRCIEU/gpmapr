% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coloc_consistency.R
\name{pleiotropic_consistency_score}
\alias{pleiotropic_consistency_score}
\title{Pleiotropic Consistency Score (PCS)}
\usage{
pleiotropic_consistency_score(
  trait_id_1,
  trait_id_2,
  coloc_groups = NULL,
  p_threshold = 5e-08,
  include_rare = FALSE
)
}
\arguments{
\item{trait_id_1}{Numeric trait ID for the first trait}

\item{trait_id_2}{Numeric trait ID for the second trait}

\item{coloc_groups}{A dataframe of coloc_groups (e.g. from traits() or trait()).
If NULL, will fetch via traits(c(trait_id_1, trait_id_2)).}

\item{p_threshold}{P-value threshold for considering a trait to have a signal at a locus.
Defaults to 5e-8.}

\item{include_rare}{Logical, whether to also count rare result loci. Defaults to FALSE.}
}
\value{
A list with:
\itemize{
\item pcs: the Pleiotropic Consistency Score (0 to 1)
\item n_shared: number of shared loci
\item m1: loci unique to trait 1
\item m2: loci unique to trait 2
\item trait_1_loci: total loci for trait 1
\item trait_2_loci: total loci for trait 2
}
}
\description{
Computes a Jaccard-style consistency score between two traits based on
colocalisation evidence at shared loci (LD blocks).

PCS = n / (n + m1 + m2)
where:
\itemize{
\item n = number of loci where both traits colocalize (share a coloc group)
\item m1 = loci where only trait 1 has a causal signal
\item m2 = loci where only trait 2 has a causal signal
}
}
