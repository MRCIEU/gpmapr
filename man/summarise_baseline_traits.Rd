% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snp_group_verification.R
\name{summarise_baseline_traits}
\alias{summarise_baseline_traits}
\title{Baseline Top Associated Traits}
\usage{
summarise_baseline_traits(
  trait_matrix,
  snp_ids = NULL,
  trait_info = NULL,
  coloc_groups = NULL,
  exclude_trait_ids = NULL,
  top_n = 20L,
  min_snps_with_signal = 5L,
  n_categories = 2L
)
}
\arguments{
\item{trait_matrix}{SNPs x traits numeric matrix (e.g. oriented trait matrix
from EBMF / verification helpers).}

\item{snp_ids}{Optional SNP ids to include. Defaults to all rows.}

\item{trait_info}{Optional dataframe with \code{feature_id}/\code{trait_id} and
\code{feature_name}/\code{trait_name} columns for labels.}

\item{coloc_groups}{Optional coloc-group dataframe used to attach
\code{trait_category} labels (and for category summaries).}

\item{exclude_trait_ids}{Optional trait ids to drop (e.g. the target trait).}

\item{top_n}{Maximum traits to return. Defaults to 20.}

\item{min_snps_with_signal}{Drop traits observed in fewer than this many SNPs
in the summarised set. Defaults to 5.}

\item{n_categories}{Number of leading trait categories to report (by summed
\code{abs_mean_z}). Defaults to 2.}
}
\value{
A list with:
\itemize{
\item traits: ranked trait dataframe (includes \code{trait_category} when available)
\item categories: top trait categories for the SNP set
}
}
\description{
Rank complex-trait columns by mean absolute effect across a set
of SNPs. Use as a verification baseline when pathway enrichment is empty or
weak: do SNP groups recover distinct trait drivers?
}
