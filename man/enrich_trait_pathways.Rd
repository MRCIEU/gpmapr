% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathways.R
\name{enrich_trait_pathways}
\alias{enrich_trait_pathways}
\title{Baseline Pathway Enrichment For A Trait}
\usage{
enrich_trait_pathways(
  trait_id,
  coloc_groups = NULL,
  p_threshold = 5e-08,
  snp_key = c("variant_id", "display_snp", "coloc_group_id"),
  sources = c("KEGG", "Reactome"),
  p_value_threshold = 0.05,
  minimum_count_in_network = 2L
)
}
\arguments{
\item{trait_id}{Numeric trait ID.}

\item{coloc_groups}{Optional coloc-group dataframe. If \code{NULL}, fetched via
\code{trait(trait_id, include_associations = TRUE)$coloc_groups}.}

\item{p_threshold}{P-value threshold for target-trait SNPs. Defaults to 5e-8.}

\item{snp_key}{Column used to identify SNPs. Defaults to \code{"variant_id"}.}

\item{sources}{Character vector of pathway sources to query separately and
bind. Defaults to \code{c("KEGG", "Reactome")}. Use \code{NULL} for a single
unfiltered \code{pathway_enrichment()} call (all sources).}

\item{p_value_threshold}{FDR threshold passed to \code{pathway_enrichment()}.}

\item{minimum_count_in_network}{Minimum overlap passed to \code{pathway_enrichment()}.}
}
\value{
A list with:
\itemize{
\item trait_id, n_snps, n_genes
\item snp_genes: SNP-gene links from \code{genes_at_snps()}
\item genes: distinct genes tested
\item pathways: enriched pathway dataframe (possibly empty)
\item summary: one-row overview
}
}
\description{
Verification helper: collect molecular QTL genes at genome-wide
significant loci for a trait and test pathway enrichment (default KEGG +
Reactome). Use before clustering / EBMF to see whether the trait gene set
has recoverable pathway signal, and which SNPs those genes map to.
}
